# Plantilla de secretos locales.
# Ubicacion del archivo real: uco-parking/infisical-secrets.env  (junto a pom.xml)
#
# Pasos:
# 1. Copia este archivo:  copy infisical-secrets.template.env infisical-secrets.env
# 2. Completa DB_PASSWORD y SMTP_PASSWORD (app password de Google, 16 caracteres)
# 3. Pon NOTIFICATION_EMAIL_ENABLED=true cuando quieras enviar correos
#
# El backend lee este archivo solo al arrancar (IntelliJ o mvnw).

DB_HOST=localhost
DB_PORT=1434
DB_NAME=ucoparking
# Usuario de aplicacion (no uses SA en produccion). Crear con: uco-parking-sql\setup-db-user.ps1
DB_USERNAME=ucoparking_app
DB_PASSWORD=CAMBIAR_AQUI

REDIS_HOST=localhost
REDIS_PORT=6379

# Notification Gateway (correo SMTP)
NOTIFICATION_EMAIL_ENABLED=false
NOTIFICATION_FROM_EMAIL=tu-correo@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=tu-correo@gmail.com
SMTP_PASSWORD=app-password-de-16-caracteres
